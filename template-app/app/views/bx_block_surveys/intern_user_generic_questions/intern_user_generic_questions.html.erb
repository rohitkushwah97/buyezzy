<!DOCTYPE html>
<html lang="en" xml:lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Questions and Answers for <%= @internship.title %></title>
  <link rel="stylesheet" href="/assets/shared_styles.css">
  <style>
    /* Page-specific styles */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      table-layout: fixed; /* Ensures columns have equal width */
    }

    table, th, td {
      border: 1px solid #ddd;
    }

    th, td {
      padding: 12px;
      text-align: center;
      word-wrap: break-word; /* Wrap long content to prevent overflow */
    }

    th {
      background-color: #2c3e50;
      color: #fff;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    tr:hover {
      background-color: #e2e2e2;
    }

    caption {
      caption-side: top;
      text-align: center;
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 10px;
      color: #2c3e50;
    }

    .no-data-message {
      font-size: 18px;
      color: #ff6347;
      text-align: center;
      margin-top: 20px;
    }

    .no-data-message strong {
      color: red;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 30px;
      font-size: 16px;
      color: #2c3e50;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="content-container">
    <h1>Questions and Answers for <%= @internship.title %></h1>

    <% if @questions_with_answers.present? %>
      <table>
        <caption>List of Questions and Answers</caption>
        <thead>
          <tr>
            <th style="width: 50%;">Question</th> <!-- Ensure both columns are equal -->
            <th style="width: 50%;">Answer</th>
          </tr>
        </thead>
        <tbody>
          <% @questions_with_answers.each do |record| %>
            <tr>
              <td><%= record.question %></td>
              <td><%= record.answer.presence || "No Answer Provided" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <p class="no-data-message">
        <strong>No questions or answers available for this internship.</strong>
      </p>
    <% end %>

    <a href="<%= request.referrer || root_path %>" class="back-link">Go Back</a>
  </div>

</body>
</html>
